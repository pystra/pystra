# Oldest supported dependency versions, installed as constraints by the
# minimum-dependencies job in .github/workflows/pytest.yml. Keep these in step
# with the lower bounds in pyproject.toml.
numpy==1.26.0
scipy==1.11.2
pandas==2.1.0
matplotlib==3.8.0
scikit-learn==1.3.1
